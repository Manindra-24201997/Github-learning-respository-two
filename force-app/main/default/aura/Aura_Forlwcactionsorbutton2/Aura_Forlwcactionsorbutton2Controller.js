({
    doInit : function(component, event, helper) {
/* let navService = component.find("navService");
        let pageReference = {
            type: "standard__navItemPage",
            attributes: {
                componentName: "c__lwc_quickactioncmp3" // this will wrap your LWC
            },
            state: {
                c__currentrcdid: component.get("v.recordId") // pass params if needed
            }
        };
        navService.navigate(pageReference, true);*/
    }
})